name: Call Reusable Auto-Pull Workflow

on:
  push:
    branches:
      - "*"

jobs:
  job1:
    uses: OmmrGAZAR315/Ansible/.github/workflows/auto-pull.yml@master3-b

  job2:
    runs-on: ubuntu-latest
    needs: job1
    env:
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    steps:
      - run: echo ${{ needs.job1.input.ssh_key }}


