---

- hosts: all
  gather_facts: yes
  become: true
  tasks:

  - name: update repository index
    apt:
      update_cache: true
    when: ansible_distribution == "Centos"

  - name: install nginx package
    apt:
      name: nginx
